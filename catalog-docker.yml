spring:
  datasource:
    url: jdbc:postgresql://catalog-db:5432/catalog
  profiles:
    active: docker

eureka:
  client:
    serviceUrl:
      defaultZone: http://eurekaserver:8070/eureka/

server:
  port: 2550
spring:
  datasource:
    url: jdbc:postgresql://customer-db:5432/customers
  profiles:
    active: docker

eureka:
  client:
    serviceUrl:
      defaultZone: http://eurekaserver:8070/eureka/

server:
  port: 2550